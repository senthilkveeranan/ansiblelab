gather_subnet directive:
========================
# ansible web -m setup -a "gather_subset=network"  --become
# ansible web -m setup -a "gather_subset=hardware"  --become

Filder directive:
==================

# ansible web -m seubset -a "Filter=ansible_devies" --become
# ansible web -m seubset -a "Filter=ansible_lvm" --become
# ansible web -m seubset -a "Filter=ansible_*name" --become
 